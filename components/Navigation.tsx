'use client';

// Navigation component — bottom tab bar removed.
// Top tab navigation is now handled by the Header component's tab strip.
// This file is kept for compatibility with layout.tsx import.

export function Navigation() {
  return null;
}
